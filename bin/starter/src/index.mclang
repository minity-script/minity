namespace ###PACK###

function hello() {
  // call this function with /function ###PACK###:hello from minecraft check
  /say Datapack ###PACK### is loaded.
}

// anything placed here will be run on load
hello() 

function on_tick minecraft:tick {
  // anything placed here will be run on every tick
}